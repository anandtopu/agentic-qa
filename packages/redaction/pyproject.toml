[project]
name = "aqao-redaction"
version = "0.0.0"
description = "Secret redaction utility used across logs, evidence, and reports"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aqao_redaction"]
