{
  "name": "aqao-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Agentic QA Orchestrator — pnpm workspace orchestrator (apps/web + future TS packages).",
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "lint": "pnpm -r --if-present lint",
    "format": "pnpm -r --if-present format",
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build",
    "dev:web": "pnpm --filter aqao-web dev"
  },
  "packageManager": "pnpm@9.12.0"
}
